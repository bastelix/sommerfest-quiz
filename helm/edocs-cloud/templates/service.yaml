apiVersion: v1
kind: Service
metadata:
  name: {{ include "edocs-cloud.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "edocs-cloud.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080
