{% extends 'layout.twig' %}

{% block config_color_styles %}{% endblock %}

{% block title %}Seite bearbeiten{% endblock %}

{% block head %}
  <link rel="stylesheet" href="{{ basePath }}/css/main.css">
  <link rel="stylesheet" href="{{ basePath }}/css/dark.css" media="(prefers-color-scheme: dark)">
  <link rel="stylesheet" href="{{ basePath }}/css/highcontrast.css">
  <link rel="stylesheet" href="{{ basePath }}/css/uikit.min.css" id="preview-uikit-css" data-preview-asset="landing" media="print">
  {% set activeNamespace = pageNamespace|default('default') %}
  {% set hasNamespaceStyles = activeNamespace is not empty and activeNamespace != 'default' %}
  {% set namespaceSegment = hasNamespaceStyles ? '/' ~ activeNamespace : '' %}
  <link
    rel="stylesheet"
    href="{{ basePath }}/css{{ namespaceSegment }}/landing.css"
    data-default-href="{{ basePath }}/css/landing.css"
    id="preview-landing-css"
    data-preview-asset="landing"
    media="print"
    onerror="if (this.dataset && this.dataset.defaultHref) { this.onerror = null; this.href = this.dataset.defaultHref; }"
  >
  <link
    rel="stylesheet"
    href="{{ basePath }}/css{{ namespaceSegment }}/topbar.landing.css"
    data-default-href="{{ basePath }}/css/topbar.landing.css"
    id="preview-landing-topbar-css"
    data-preview-asset="landing"
    media="print"
    onerror="if (this.dataset && this.dataset.defaultHref) { this.onerror = null; this.href = this.dataset.defaultHref; }"
  >
  <link
    rel="stylesheet"
    href="{{ basePath }}/css{{ namespaceSegment }}/onboarding.css"
    data-default-href="{{ basePath }}/css/onboarding.css"
    id="preview-landing-onboarding-css"
    data-preview-asset="landing"
    media="print"
    onerror="if (this.dataset && this.dataset.defaultHref) { this.onerror = null; this.href = this.dataset.defaultHref; }"
  >
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/trumbowyg@2/dist/ui/trumbowyg.min.css">
  <script src="https://cdn.jsdelivr.net/npm/jquery@3/dist/jquery.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/trumbowyg@2/dist/trumbowyg.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/dompurify@3/dist/purify.min.js"></script>
  <script>
    window.pageNamespace = '{{ activeNamespace|e('js') }}';
  </script>
  <script type="module" src="{{ basePath }}/js/trumbowyg-pages.js"></script>
{% endblock %}

{% block body_class %}uk-background-muted uk-padding{% endblock %}

{% set excludedLandingSlugs = ['impressum', 'datenschutz', 'faq', 'lizenz'] %}

{% block body %}
  <div class="uk-container uk-container-expand">
    <h1 class="uk-heading-bullet">Seite bearbeiten: {{ slug }}</h1>
    <form
      method="post"
      class="uk-form-stacked page-form"
      data-slug="{{ slug }}"
      data-landing="{{ slug not in excludedLandingSlugs ? 'true' : 'false' }}"
      data-page-namespace="{{ activeNamespace|e('html_attr') }}"
    >
      <input type="hidden" id="pageContent" name="content" value="{{ content|e('html_attr') }}">
      <div class="page-editor" data-content="{{ content|e('html_attr') }}"></div>
      <div class="uk-margin-top">
        <button type="button" class="uk-button uk-button-primary save-page-btn">Speichern</button>
        <button type="button" class="uk-button uk-button-default" onclick="showPreview()">Vorschau</button>
      </div>
    </form>

    <div id="preview-modal" uk-modal>
      <div class="uk-modal-dialog uk-modal-body">
        <h3>Vorschau</h3>
        <div id="preview-content"></div>
        <button class="uk-button uk-button-default uk-modal-close" type="button">Schließen</button>
      </div>
    </div>
  </div>
  {# Page editor initialization handled by trumbowyg-pages.js #}
{% endblock %}
