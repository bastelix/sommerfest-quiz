{% extends 'admin/base.twig' %}
{% set includeMarketingThemeVars = true %}

{% block title %}{{ t('tab_project_overview') }}{% endblock %}

{% block content %}
  <div class="uk-container uk-container-large">
    <h2 class="uk-heading-bullet">{{ t('heading_namespace_overview') }}</h2>
  </div>
  <div class="uk-container uk-container-large">
    {% set projectKpis = [
      { key: 'pages', label: t('label_kpi_pages') },
      { key: 'wiki', label: t('label_kpi_wiki_articles') },
      { key: 'news', label: t('label_kpi_news_articles') },
      { key: 'newsletter', label: t('label_kpi_newsletter_slugs') },
      { key: 'media', label: t('label_kpi_media_refs') }
    ] %}
    <div
      class="uk-grid-small uk-child-width-1-2@s uk-child-width-1-5@m uk-margin"
      uk-grid
      data-project-kpis
    >
      {% for kpi in projectKpis %}
        <div>
          <div class="uk-card uk-card-default uk-card-body uk-text-center">
            <div class="uk-text-large uk-text-bold" data-project-kpi="{{ kpi.key }}">0</div>
            <div class="uk-text-meta">{{ kpi.label }}</div>
          </div>
        </div>
      {% endfor %}
    </div>

    {% include 'admin/components/project_tree.twig' %}
  </div>
{% endblock %}

{% block scripts %}
  <script type="module" src="{{ basePath }}/js/admin.js"></script>
{% endblock %}
