{% extends 'emails/layout.twig' %}

{% block content %}
{% set heading = headline|default('Fast geschafft!') %}
{% set actionLabel = action_label|default('Bestätigung abschließen') %}
{% set displayName = player_name|default('Quiz-Fan') %}

<table role="presentation" cellpadding="0" cellspacing="0" width="100%" style="background:#000000;border-radius:16px;box-shadow:0 12px 35px rgba(15,23,42,0.18);overflow:hidden;">
  <tr>
    <td style="padding:40px 32px 28px 32px;">
      <p style="margin:0;font-size:14px;letter-spacing:0.12em;text-transform:uppercase;color:rgba(255,255,255,0.7);">QuizRace</p>
      <h1 style="margin:12px 0 16px;font-size:28px;line-height:1.25;color:#ffffff;font-family:'Segoe UI',Roboto,Helvetica,Arial,sans-serif;">{{ heading }}</h1>
      <p style="margin:0;font-size:16px;line-height:1.6;color:rgba(255,255,255,0.9);">
        Hallo {{ displayName }},
        {% if event_name %}
          du hast dich für das Ranking der Veranstaltung <strong style="color:#ffffff;">{{ event_name }}</strong> eingetragen.
        {% else %}
          du möchtest deine Platzierung im Ranking sichern.
        {% endif %}
        Um deine E-Mail-Adresse zu bestätigen, klick bitte auf den folgenden Button.
      </p>
      <p style="margin:28px 0 0;">
        <a
          href="{{ link }}"
          style="display:inline-block;background:#ffffff;color:#1a73e8;padding:14px 28px;border-radius:999px;font-weight:600;text-decoration:none;font-size:16px;box-shadow:0 10px 25px rgba(15,23,42,0.25);"
        >{{ actionLabel }}</a>
      </p>
    </td>
  </tr>
  <tr>
    <td style="padding:24px 32px;background:#ffffff;">
      <p style="margin:0 0 12px;font-size:15px;line-height:1.6;color:#1f2937;">
        Falls der Button nicht funktioniert, kopiere diesen Link und füge ihn in deinem Browser ein:
      </p>
      <p style="margin:0;font-size:14px;line-height:1.6;word-break:break-word;">
        <a href="{{ link }}" style="color:#1a73e8;text-decoration:none;">{{ link }}</a>
      </p>
      <p style="margin:20px 0 0;font-size:13px;line-height:1.6;color:#6b7280;">
        Wenn du diese Anfrage nicht gestellt hast, ignoriere diese Nachricht einfach. Deine Daten bleiben dann unverändert.
      </p>
    </td>
  </tr>
</table>
{% endblock %}
