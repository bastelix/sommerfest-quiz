{
  "name": "sommerfest-quiz",
  "version": "1.0.0",
  "description": "Admin- und Marketing-Frontend für QuizRace",
  "main": "index.js",
  "directories": {
    "doc": "docs",
    "test": "tests"
  },
  "scripts": {
    "test": "npm run test:unit && npm run test:playwright",
    "test:unit": "vitest run",
    "test:unit:watch": "vitest",
    "test:unit:coverage": "vitest run --coverage",
    "test:playwright": "playwright test",
    "lint": "npm run lint:php && npm run lint:phpstan && npm run test:unit && npm run lint:pytest",
    "lint:php": "vendor/bin/phpcs",
    "lint:phpstan": "vendor/bin/phpstan",
    "test:phpunit:unit": "vendor/bin/phpunit --exclude-group integration --no-coverage",
    "lint:pytest": "python3 -m pytest tests/test_html_validity.py tests/test_json_validity.py",
    "prepare": "husky"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@commitlint/cli": "^19.6.1",
    "@commitlint/config-conventional": "^19.6.0",
    "@playwright/test": "^1.57.0",
    "@vitest/coverage-v8": "^3.2.4",
    "husky": "^9.1.7",
    "jsdom": "^28.1.0",
    "vitest": "^3.2.4"
  }
}
